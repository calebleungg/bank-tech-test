Add support for multiple accounts per customer

An account would have a balance
A user can have different accounts with different balances
Transaction logic would move from the User to the Account class
